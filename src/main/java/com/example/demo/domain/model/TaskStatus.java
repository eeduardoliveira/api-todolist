package com.example.demo.domain.model;

/**
 * Enum representing the possible states of a task in the to-do list
 * @author system
 */
public enum TaskStatus {
    PENDING,
    IN_PROGRESS,
    COMPLETED
}
